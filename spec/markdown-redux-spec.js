"use babel";

import MarkdownRedux from '../lib/markdown-redux';

describe("MarkdownRedux", function() {
  return it("has one valid test", function() {
    return expect(MarkdownRedux).toBe('markdown-redux core loaded');
  });
});
